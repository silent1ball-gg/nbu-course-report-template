\section{结果与讨论}

这里写主要结果、图表和讨论。

插入图片时，将最终图片放在 \texttt{figures/final/}，然后使用：

\begin{figure}[htbp]
  \centering
  \fbox{\parbox[c][4cm][c]{0.75\linewidth}{\centering 这里替换为图片：\texttt{\string\includegraphics}}}
  \caption{结果图片示例}
  \label{fig:result-example}
\end{figure}

如图 \ref{fig:result-example} 所示，这里可以解释结果的物理意义或课程相关含义。
